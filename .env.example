# Frontend environment example for configuring the external Diaryx backend (Elysia)
# Copy this file to `.env` (or `.env.local`) at the project root and adjust values as needed.

# ------------------------------------------------------------------------------------
# REQUIRED FOR PRODUCTION
# ------------------------------------------------------------------------------------

# Base URL for the Diaryx backend API (Elysia).
# The frontend will call `${VITE_DIARYX_API_BASE_URL}/api/...`
VITE_DIARYX_API_BASE_URL=https://api.diaryx.net

# Base URL for Better Auth handler.
# The auth client will call `${VITE_BETTER_AUTH_URL}/api/auth/...`
VITE_BETTER_AUTH_URL=https://api.diaryx.net

# ------------------------------------------------------------------------------------
# LOCAL DEVELOPMENT (UNCOMMENT AS NEEDED)
# ------------------------------------------------------------------------------------
# If running the backend locally with Bun/Elysia:
# VITE_DIARYX_API_BASE_URL=http://localhost:3000
# VITE_BETTER_AUTH_URL=http://localhost:3000

# ------------------------------------------------------------------------------------
# OPTIONAL ALTERNATE KEYS (THE CLIENT ALSO CHECKS THESE; FIRST FOUND WINS)
# ------------------------------------------------------------------------------------
# DIARYX_API_BASE_URL=https://api.diaryx.net
# VITE_API_BASE_URL=https://api.diaryx.net
# API_BASE_URL=https://api.diaryx.net

# ------------------------------------------------------------------------------------
# NOTES
# ------------------------------------------------------------------------------------
# - Ensure the backend sets cookies with `SameSite=None; Secure` when using cross-origin auth.
# - For production, use HTTPS (required for Secure cookies).
# - Frontend API calls include credentials by default for session-based auth.
